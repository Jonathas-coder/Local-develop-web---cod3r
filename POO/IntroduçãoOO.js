//antigamente tudo era precedural, função que executava dados

function get (value1,vlue2,value3)

//today everything has changed, now in POO, objecet >> function

Object = {
    value1:2,
    value3:1,
    function()
}

Object.function()
/*
1.abstração.    dos atributos no objeto;
2.encapsulamento.     entregar uma interface simples sem expor a complexidade dos objetos;
3.herança (prototype)       valor de tal objeto generico para outros objetos filhos em cadeia;
4. polimorfismo      um atributo mestre que serve pra qualquer objeto com aquele tipo;
*/
